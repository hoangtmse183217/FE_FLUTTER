/// Enum cho các tùy chọn sắp xếp (FE-only)
enum SortOption {
  latest, // Mới nhất (mặc định từ API)
  nameAZ,
  nameZA,
  priceAsc,
  priceDesc,
  ratingDesc,
  distance, // Sắp xếp theo khoảng cách gần nhất
}
